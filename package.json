{
  "devDependencies": {
    "babel-cli": "^6.0.0"
  },
  "scripts": {
    "build": "babel --no-comments -Dd www src",
    "watch": "babel --no-comments -wDd www src",
    "update": "tools/UpdateResources",
    "release": "cordova build android"
  }
}
