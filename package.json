{
  "devDependencies": {
    "babel-cli": "^6.0.0"
  },
  "scripts": {
    "build": "babel --no-comments --presets es2015 --ignore src/lib/ -Dd www src",
    "watch": "babel --no-comments -wDd www src",
    "update": "tools\\UpdateResources.bat",
    "release": "cordova build android"
  }
}
