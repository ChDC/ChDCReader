define(["jquery", "util"], function($, util) {
    "use strict"

    // 响应式的 CSS 类
    var reactCSS = {
        init: {

            $(window).on("resize", function(){
                // 当窗口尺寸改变的时候改变某些元素的尺寸
                $(".full-height").css('height', )
            });
        }
    };
    return reactCSS;
});
